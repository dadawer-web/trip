1.SDD 你现在是一个资深的后端架构师。我们要开发一个名为“100种不可思议旅行”的 Web App MVP。后端采用 Python FastAPI + SQLite。 请采用 SDD (Spec-Driven Development) 模式为我设计底层架构：

1. 请设计 SQLite 的数据库表结构，必须包含：旅行地名称、核心标签、情绪描述、高分辨率图片链接等字段。
2. 请提供对应的 FastAPI 路由接口契约（比如 GET /api/trips 用于列表渲染和标签筛选）。
3. 请将以上内容输出为一份清晰的 Markdown 文档，命名为 `schema-design.md`，并在文档中包含 Mermaid 格式的 ER 图。 注意：此阶段你只需生成并保存设计文档，绝对不要生成任何实际的 Python 业务代码，也不要修改其他文件。

2.现在进入 DDD (Design-Driven Development) 阶段。请基于我们刚刚在 `schema-design.md` 中定义的 Trip 数据结构，进行前端组件的设计与代码生成。 我们采用纯 HTML + Tailwind CSS (通过 CDN 引入) + Vanilla JavaScript 来构建轻量级前端。

请帮我在项目根目录下直接创建并写入一个 `index.html` 文件，要求满足‘UI UX Pro Max’的标准：

1. **整体视觉**：采用高级质感的暗色调背景，充满‘Vibe’氛围感和视觉冲击力。
2. **核心组件（TripCard）**：实现一个精致的旅行地展示卡片。必须包含：大面积的高清图片占位、悬浮时的平滑放大/发光过渡动画（Framer Motion 风格的丝滑感）、显眼的‘情绪标签’（如：寻找松弛感）。
3. **交互组件**：顶部需要有一排支持横向滑动的‘情绪过滤’按钮。
4. **Mock数据驱动**：在 JS 脚本中，请严格按照 Schema 结构，生成 **5条极其高质量的 Mock 数据**（请用心编写真实的风景地名称和走心的情绪文案，配上高质量的 Unsplash 随机图片链接），并用这 5 条数据将页面渲染出来。 注意：此阶段直接生成并保存完整的 `index.html` 文件代码，暂时不需要对接真实的后端 API。”

3.现在进入 TDD (Test-Driven Development) 阶段。我们需要为 FastAPI 后端实现‘根据情绪标签筛选旅行地’的核心逻辑。 **请注意：严禁修改或生成任何核心业务代码！** 请先为我创建一个 `test_main.py` 文件，使用 `pytest` 编写针对 `GET /api/trips?tag=xxx` 接口的单元测试。测试必须包含：1. 正常匹配标签；2. 传入不存在的标签返回空；3. 不传参返回全部列表。请直接输出测试代码。”

4.测试用例已就绪。现在请帮我编写 `main.py` 中的 FastAPI 业务逻辑，实现对应的数据库查询和筛选功能，确保刚刚的 `pytest` 测试能够 100% 运行通过。请保证代码整洁并处理好跨域 (CORS) 问题。

5.现在进入最终的 E2E 阶段。请使用 Python 版本的 Playwright 生成一个端到端测试脚本 `e2e_test.py`。 测试剧本如下：

1. 模拟用户打开本地的 `index.html` 页面。
2. 定位并点击顶部筛选栏里的‘寻找松弛感’按钮（或其他你 mock 的真实标签）。
3. 验证页面卡片是否发生了改变，且正确渲染了对应标签的数据。 请直接输出 Playwright 脚本代码，并添加清晰的注释。”

6.这是最后一次逻辑优化。请修改 `index.html` 中的 JavaScript 部分：

1. 删掉写死的 Mock 数据。
2. 使用 `fetch` API 在页面初始化时调用 `http://127.0.0.1:8080/api/trips` 获取全部数据。
3. 当用户点击标签时，带上查询参数（如 `?tag=xxx`）再次 fetch 接口并更新 DOM。
4. 增加简单的 Loading 状态提示，增强交互体验。

7.你刚才生成的 `main.py` 只是一个空壳，这会导致我的前端页面无法渲染。

请立刻重构 `main.py`，要求如下：

1. **注入灵魂数据**：在代码中直接定义一个名为 `TRIPS_DATA` 的列表，包含 **5 条极具视觉冲击力和情绪价值**的旅行数据（严格遵循 `schema-design.md` 结构：id, name, description, tags, image_url）。
   - *要求：文案要走心，图片使用高质量的 Unsplash 链接。*
2. **实现真筛选逻辑**：重写 `get_trips` 函数。如果没有传 `tag`，返回全部数据；如果传了 `tag`，则根据标签进行模糊匹配或精确匹配。
3. **确保测试通过**：逻辑必须能够支撑之前写的 `test_main.py` 中的所有测试用例。
4. **保持 CORS 开启**：确保前端能正常访问。

请直接输出完整的、可运行的 `main.py` 代码，不要写占位符。

。。。。。。。。。。。。。。。。。。。。。。。。。。。。。。。。。。。。。。。。。

**发现问题**：AI 生成了过度抽象的接口空壳，未包含初始 Mock 数据，导致前后端联调失败。

**纠偏行动**：下达“注入灵魂数据”指令，要求 AI 将设计文档中的契约转化为硬编码的业务初值，以满足 MVP 演示需求。

8.请为我生成一个单独的 Python 脚本 `init_db.py`，要求如下：

1. **创建连接**：在当前目录下创建名为 `trips.db` 的 SQLite 数据库。
2. **建立表结构**：根据之前的 `schema-design.md`，创建 `trips` 表（包含 id, name, description, tags, image_url）。
3. **注入灵魂种子数据**：将那 5 条高质量的旅行地 Mock 数据直接通过 SQL 插入到表中，确保数据库一启动就有内容。

请直接输出完整的 `init_db.py` 代码。生成后，我会运行它来初始化环境。

现在我们需要把硬编码的数据变成SQLITE里面的

9. 现在数据库 `trips.db` 已经初始化好了。

   请重构 `main.py`，要求：

   1. **连接真实数据库**：使用 Python 内置的 `sqlite3` 库（或你建议的 ORM）连接到 `trips.db`。
   2. **实现真查询逻辑**：修改 `get_trips` 函数，通过 `SELECT` 语句从数据库中查询数据。
   3. **实现真筛选逻辑**：当传入 `tag` 参数时，使用 `WHERE tags LIKE ?` 这种 SQL 语句进行过滤。
   4. **保持异步兼容**：确保 FastAPI 的路由依然能够高效返回 JSON 数据。

   请输出重构后的完整 `main.py` 代码。

10.此时我发现有逻辑问题，虽然引入了数据库，按理说前端html应该去sqlite里面找图，但是ai还是写死了url，使5张图片都一样同时硬编码数据

我们的后端 FastAPI 已经重构完毕，成功连接了 SQLite 数据库，目前在 `http://127.0.0.1:8080/api/trips` 提供了真实的接口（支持通过 `?tag=xxx` 筛选，返回格式为 `{"items": [...], "total": 5}`）。

现在我们需要进行全链路联调。请帮我修改 `index.html` 中底部的 `<script>` 逻辑部分，要求如下：

1. **删除旧逻辑**：彻底删除之前写死的 Mock 数据列表。
2. **实现动态请求**：使用原生 `fetch` API 编写一个异步函数 `fetchTrips(tag = '')`。页面首次加载时请求全部数据，当用户点击标签时，带上对应的参数（如 `?tag=hotspring`）请求筛选数据。
3. **动态渲染 DOM**：拿到接口返回的 `items` 数据后，使用 JavaScript 动态拼接 HTML 字符串，将卡片（包含图片的 `cover_image_url`、名称、描述和标签）渲染到原本的卡片容器中。
4. **交互优化**：在 `fetch` 期间，在页面上展示一个简单的“加载中... (Loading)”状态，增强 Vibe 视觉体验。

请直接输出重构后的完整 `<script> ... </script>` 代码块。

11.硬编码问题解决，但是前端5张图片都是一样，我复查ai的代码发现，后端/数据库端图片的字段名叫做 image_url，但是前端 JS 在尝试读取 cover_image_url，触发了同一张“海滩大巴”兜底图

现在的项目存在 **SDD (契约) 与实现层面的严重脱节**，导致前端渲染逻辑回退到了 Mock 状态。我们需要进行一次全方位的‘工程对齐’与‘视觉重塑’：

1. **对齐 Schema (SDD)**：
   - 请检查并重写 `init_db.py`。确保数据库字段与 `schema-design.md` 严格一致。特别是将 `image_url` 统一改为 `cover_image_url`。
   - 在 `SEED_TRIPS` 中补全 `emotion_description` (情绪文案)、`country` (国家)、`best_season` (季节) 以及 `is_featured` 字段。
   - 重新运行初始化，确保 `trips.db` 拥有完整的‘灵魂数据’。
2. **重构后端 (API Logic)**：
   - 修改 `main.py`。确保查询结果返回的 JSON 键名与新 Schema 匹配。
   - 优化 `tags` 的处理逻辑，确保它以标准的 JSON List 格式输出给前端。
3. **视觉超进化 (Vibe DDD)**：
   - 优化 `index.html` 的 CSS。请在卡片上增加 **Glassmorphism (玻璃拟态)** 效果：增加 `backdrop-filter: blur(12px)` 和微弱的白色描边。
   - 改进动画：使用 **Web Animations API** 或更精细的 CSS Transition，让卡片在加载时呈现‘由下至上、由暗到明’的丝滑入场感。
   - **修复 Bug**：修改前端 JS 逻辑，确保 `fetch` 到的 `cover_image_url` 能正确赋值给 `<img>` 标签，彻底干掉那个兜底的默认图。
4. **工程闭环 (TDD)**：
   - 更新 `test_main.py`。确保测试用例能覆盖新增的字段。

**目标**：我要在浏览器刷新的一瞬间，看到 5 张不同质感的、具备高级审美冲击力的旅行卡片从暗影中浮现。